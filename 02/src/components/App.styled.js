import styled from 'styled-components';

const StyledApp = styled.article`
    padding: 2rem;
    margin: auto;
    max-width: 1200px;
    background-color: rgb(var(--color-beta));
    box-shadow: 0px 0px 2px rgb(var(--color-alfa));
    color: rgb(var(--color-font)); 
`

export default StyledApp;